---
import NavDock from "@/components/NavDock.astro";
import "../styles/global.css";
import { ClientRouter } from "astro:transitions";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/logov200.png" />
        <meta name="generator" content={Astro.generator} />
        <title>ᓚ₍ ^. .^₎</title>
        <ClientRouter />
        <script>
            import { init_pathchange_detection } from "@/components/core";
            import { initYoutubeApi } from "@/lib/external/yt";

            init_pathchange_detection();
            initYoutubeApi();
            // window.scrollTo({ top: 0 });
            // document.addEventListener("pathchanged", () => {
            //     // ----- Hash scrolling ------
            //     // Skip if no hash
            //     if (location.hash.length < 1) return;
            //     // If window has been scrolled, DO NOT continue with default scroll behaviour
            //     if (window.scrollY > 0) return;
            //     const target = document.querySelector(location.hash);
            //     if (!target) return;
            //     target.scrollIntoView({
            //         behavior: "smooth",
            //     });
            // });
        </script>
    </head>
    <body id="top">
        <slot />
    </body>
</html>

<style>
    html,
    body {
        margin: 0;
        width: 100%;
    }
</style>
